function validateCartCount(expectedCount) {
    // const actual = await page.locator('.cart_badge').textContent();
    // assert.equal(actual, expectedCount);
}

function validateCartItems(products) {
    // Loop through products and check cart page
}

module.exports = { validateCartCount, validateCartItems };
